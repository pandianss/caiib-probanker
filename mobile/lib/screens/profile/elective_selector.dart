import 'package:flutter/material.dart';

class ElectiveSelectorScreen extends StatelessWidget {
  const ElectiveSelectorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Elective Selector Screen')));
  }
}
