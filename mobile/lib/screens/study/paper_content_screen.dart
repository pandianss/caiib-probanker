import 'package:flutter/material.dart';

class PaperContentScreen extends StatelessWidget {
  const PaperContentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Paper Content Screen')));
  }
}
